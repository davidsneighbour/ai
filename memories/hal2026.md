# hal2026 — Raspberry Pi profile

> AI memory/reference file for the machine `hal2026`.
>
> Prefer observed machine facts in this document over generic Raspberry Pi defaults. Model-level hardware capabilities are included for context and should not be interpreted as confirmation that a particular peripheral is currently connected or configured.

## Identity

- Hostname: `hal2026`
- Device class: Raspberry Pi single-board computer
- Exact model: Raspberry Pi 4 Model B Rev 1.5
- Board revision code: `d03115`
- Architecture: `arm64` / `aarch64`
- Primary administrator: `patrick`
- Timezone: `Asia/Bangkok` (ICT, UTC+7)
- System clock synchronization: enabled
- NTP: active
- SSH: enabled and active

## Hardware

### Board

- Model: Raspberry Pi 4 Model B Rev 1.5
- SoC family: Broadcom BCM2711
- CPU: quad-core ARM Cortex-A72
- CPU architecture: ARMv8, 64-bit
- CPU cores: 4
- Threads per core: 1
- Observed CPU frequency range: 600 MHz–1.8 GHz
- L1 data cache: 128 KiB total
- L1 instruction cache: 192 KiB total
- L2 cache: 1 MiB
- Installed RAM: 8 GB class
- Observed usable RAM: approximately 7.6 GiB (`8007464 kB` MemTotal)
- GPU family: VideoCore VI

### Built-in connectivity

- Gigabit Ethernet
- Dual-band 2.4 GHz / 5 GHz IEEE 802.11ac Wi-Fi
- Bluetooth 5.0
- Bluetooth Low Energy (BLE)

### Physical I/O and expansion

The Raspberry Pi 4 Model B provides:

- 2 × USB 3.0 Type-A ports
- 2 × USB 2.0 Type-A ports
- 2 × micro-HDMI outputs
- Dual-display support
- Up to 4K display output
- 40-pin Raspberry Pi GPIO header
- 2-lane MIPI CSI camera connector
- 2-lane MIPI DSI display connector
- 4-pole 3.5 mm stereo audio and composite-video jack
- microSD card slot
- USB-C power input
- Gigabit Ethernet port
- PoE capability via an appropriate PoE HAT

Do not assume GPIO, SPI, I2C, camera, display, Bluetooth, or Wi-Fi peripherals are physically connected merely because the hardware supports them.

## Operating system

- Distribution: Debian GNU/Linux 13
- Codename: `trixie`
- Debian version observed: `13.5`
- Kernel: `6.18.34+rpt-rpi-v8`
- Kernel build: `#1 SMP PREEMPT Debian 1:6.18.34-1+rpt1 (2026-06-09)`
- Userland architecture: `arm64`
- Machine architecture: `aarch64`
- Firmware build observed: 2026-05-21
- Firmware version: `288930ab4712b99596f32732664aaaeb881ef1e0`
- Firmware state when profiled: clean release build
- Throttling status when profiled: `throttled=0x0` (no throttling/undervoltage flags reported)

## Storage

Primary storage is a microSD card.

- Device: `/dev/mmcblk0`
- Nominal size: approximately 32 GB
- Observed device size: 28.9 GiB
- Boot partition:
  - Device: `/dev/mmcblk0p1`
  - Filesystem: FAT
  - Size: 512 MiB
  - Mountpoint: `/boot/firmware`
- Root partition:
  - Device: `/dev/mmcblk0p2`
  - Filesystem: ext4
  - Size: 28.4 GiB
  - Mountpoint: `/`
- At profiling time:
  - Root filesystem usage: approximately 6.8 GiB used
  - Approximately 20 GiB available
  - Approximately 26% used
- Swap:
  - 2 GiB zram-backed swap
  - No swap usage at profiling time

## Networking

Network management is handled by NetworkManager.

### Ethernet

- Interface: `eth0`
- State when profiled: up and connected
- MAC address: `d8:3a:dd:1a:d4:9a`
- IPv4 address: `192.168.1.9/24`
- Address source: DHCP
- Desired permanent LAN address: `192.168.1.9`
- LAN: `192.168.1.0/24`
- Default gateway: `192.168.1.1`
- NetworkManager connection: `Wired connection 1`
- Recommended IP persistence method: DHCP reservation on the router keyed to Ethernet MAC `d8:3a:dd:1a:d4:9a`
- Do not configure a second static address on the Pi if the router DHCP reservation is in use.

Expected LAN SSH access:

```bash
ssh patrick@192.168.1.9
```

Expected mDNS SSH access:

```bash
ssh patrick@hal2026.local
```

### Wi-Fi

- Interface: `wlan0`
- MAC address: `d8:3a:dd:1a:d4:9b`
- State when profiled: down / unavailable
- No active NetworkManager Wi-Fi connection was present when profiled.
- The Raspberry Pi 4 hardware supports dual-band 2.4/5 GHz 802.11ac Wi-Fi.
- Do not assume the originally supplied Raspberry Pi Imager Wi-Fi settings were applied: initial Imager customisation failed on this installation.

## SSH and remote administration

- OpenSSH server: enabled
- OpenSSH server state when profiled: active
- Administrative user: `patrick`
- `patrick` UID: `1000`
- `patrick` GID: `1000`
- `patrick` has sudo privileges.

Observed supplementary groups:

- `adm`
- `dialout`
- `cdrom`
- `sudo`
- `audio`
- `video`
- `plugdev`
- `games`
- `users`
- `input`
- `render`
- `netdev`
- `spi`
- `i2c`
- `gpio`
- `lpadmin`

The account therefore has normal system administration privileges plus access to common Raspberry Pi hardware interfaces.

## Time and locale

- Timezone: `Asia/Bangkok`
- Timezone abbreviation: ICT
- UTC offset: +07:00
- Daylight saving time: not used
- NTP synchronization: active
- Hardware RTC: none reported (`RTC time: n/a`)
- RTC is not configured to local time.

## Boot and health notes

At the time this profile was generated:

- Firmware reported `throttled=0x0`.
- This means no current or historical undervoltage/throttling flags were reported by `vcgencmd get_throttled`.
- SSH was enabled and active.
- Ethernet was up.
- Wi-Fi was down.
- System clock was synchronized.

Useful health commands:

```bash
vcgencmd get_throttled
vcgencmd measure_temp
uptime
free -h
df -hT
ip -brief address
systemctl --failed
```

## Installation history and caveats

This installation was initially prepared using Raspberry Pi Imager with custom settings including:

- custom user
- hostname
- SSH
- Wi-Fi credentials

Those Imager customisations did not apply correctly on first boot.

Recovery steps included:

- manually enabling SSH using the boot partition
- manually provisioning the `patrick` user
- granting `patrick` sudo/admin privileges
- changing the hostname to `hal2026`
- changing the timezone to `Asia/Bangkok`

Because multiple Imager customisations failed, do not assume any setting requested during the original imaging process exists unless it has subsequently been verified.

In particular, Wi-Fi configuration should be treated as unconfigured until explicitly checked or recreated.

## AI assistant guidance

When providing commands or configuration specifically for `hal2026`:

- Assume Debian 13 (`trixie`), not an older Raspberry Pi OS/Debian release.
- Assume a 64-bit ARM (`arm64` / `aarch64`) system.
- Use `patrick` as the administrative user.
- Use `sudo` for privileged operations.
- Prefer `apt` for operating-system packages.
- Treat NetworkManager as the network configuration layer and prefer `nmcli` for host-side network changes.
- Prefer the router's DHCP reservation for maintaining Ethernet address `192.168.1.9`.
- Prefer `Asia/Bangkok` as the system timezone.
- Use `/boot/firmware` rather than legacy `/boot` paths when referring to the active boot partition.
- Do not assume Wi-Fi is configured.
- Do not assume the original Raspberry Pi Imager customisations succeeded.
- Take the 8 GB RAM capacity into account when recommending workloads.
- Remember that binaries installed or compiled for x86-64 will not run natively; this system requires ARM64-compatible software.
- Raspberry Pi hardware-specific commands such as `vcgencmd` are available.
- The machine has GPIO/SPI/I2C-capable hardware and the `patrick` user belongs to the corresponding access groups.
- Avoid storing or repeating the board serial number unless explicitly needed for diagnostics or identification.

## Useful identification commands

```bash
hostnamectl
cat /sys/firmware/devicetree/base/model; echo
grep -E '^(Model|Revision)' /proc/cpuinfo
uname -a
cat /etc/os-release
free -h
lsblk -f
ip -brief address
ip -brief link
nmcli device status
timedatectl
vcgencmd version
vcgencmd get_throttled
```

## Profile provenance

This profile combines:

1. Facts observed directly from `hal2026` on 2026-08-20.
2. Stable Raspberry Pi 4 Model B hardware capabilities from Raspberry Pi's official published specifications.

Values describing current state — IP addresses, disk usage, kernel, firmware, connection state, service state, and package versions — may change and should be re-queried when exact current-state information matters.
