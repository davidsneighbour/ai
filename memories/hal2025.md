# hal2025 — Raspberry Pi profile

> AI memory/reference file for the machine `hal2025`.
>
> Prefer observed machine facts in this document over generic Raspberry Pi defaults. Model-level hardware capabilities are included for context and should not be interpreted as confirmation that a particular peripheral is currently connected or configured.

## Identity

- Hostname: `hal2025`
- Device class: Raspberry Pi single-board computer
- Exact model: Raspberry Pi 5 Model B Rev 1.0
- Board revision code: `c04170`
- Architecture: `arm64` / `aarch64`
- Primary administrator: `patrick`
- Timezone: `Asia/Bangkok` (ICT, UTC+7)
- System clock synchronization: enabled
- NTP: active
- SSH: enabled and active

## Hardware

### Board

- Model: Raspberry Pi 5 Model B Rev 1.0
- SoC family: Broadcom BCM2712
- CPU: quad-core ARM Cortex-A76
- CPU architecture: ARMv8, 64-bit
- CPU cores: 4
- Threads per core: 1
- Observed CPU frequency range: 1.5 GHz–2.4 GHz
- L1 data cache: 256 KiB total
- L1 instruction cache: 256 KiB total
- L2 cache: 2 MiB × 4 instances
- L3 cache: 2 MiB
- Installed RAM: 4 GB class
- Observed usable RAM: approximately 4.0 GiB (`4147008 kB` MemTotal)
- GPU family: VideoCore VII

### Built-in connectivity

- Gigabit Ethernet
- Dual-band 2.4 GHz / 5 GHz IEEE 802.11ac Wi-Fi
- Bluetooth 5.0
- Bluetooth Low Energy (BLE)

### Physical I/O and expansion

The Raspberry Pi 5 Model B provides:

- 2 × USB 3.0 Type-A ports
- 2 × USB 2.0 Type-A ports
- 2 × micro-HDMI outputs
- Dual-display support
- 40-pin Raspberry Pi GPIO header
- 2 × four-lane MIPI camera/display transceivers
- PCI Express 2.0 x1 FFC connector
- Real-time clock support
- Dedicated fan connector
- microSD card slot
- USB-C power input
- Gigabit Ethernet port
- PoE capability via an appropriate PoE+ HAT

Do not assume GPIO, SPI, I2C, camera, display, Bluetooth, Wi-Fi, PCIe, or other peripherals are physically connected merely because the hardware supports them.

## Operating system

- Distribution: Debian GNU/Linux 12
- Codename: `bookworm`
- Kernel: `6.12.75+rpt-rpi-2712`
- Kernel build: `#1 SMP PREEMPT Debian 1:6.12.75-1+rpt1~bookworm (2026-03-11)`
- Userland architecture: `arm64`
- Machine architecture: `aarch64`

## Storage

Primary system storage is an NVMe SSD.

- Device: `/dev/nvme0n1`
- Model string: `PCIe SSD`
- Observed size: 476.9 GiB
- Boot partition:
  - Device: `/dev/nvme0n1p1`
  - Filesystem: FAT
  - Size: 512 MiB
  - Mountpoint: `/boot/firmware`
- Root partition:
  - Device: `/dev/nvme0n1p2`
  - Filesystem: ext4
  - Size: 476.4 GiB
  - Mountpoint: `/`
- At profiling time:
  - Root filesystem usage: approximately 76 GiB used
  - Approximately 370 GiB available
  - Approximately 17% used

### Network storage

A remote NFSv4 filesystem is mounted:

- Remote: `192.168.1.201:/mnt/storage`
- Local mountpoint: `/mnt/storage`
- Size reported: approximately 19 TiB
- Used: approximately 7.6 TiB
- Available: approximately 9.7 TiB
- Usage: approximately 44%

## Memory and swap

- Installed RAM: 4 GB class
- Observed memory total: approximately 4.0 GiB
- At profiling time:
  - approximately 1.2 GiB used
  - approximately 2.8 GiB available
- Swap:
  - approximately 199 MiB configured
  - approximately 99 MiB in use at profiling time

## Networking

Network management is handled by NetworkManager.

### Ethernet

- Interface: `eth0`
- State when profiled: up and connected
- MAC address: `2c:cf:67:6b:1e:91`
- IPv4 address: `192.168.1.21/24`
- LAN: `192.168.1.0/24`
- Default gateway: `192.168.1.1`
- Default route source: static
- NetworkManager connection: `Wired connection 1`

Expected LAN SSH access:

```bash
ssh patrick@192.168.1.21
```

Expected mDNS SSH access:

```bash
ssh patrick@hal2025.local
```

### Wi-Fi

- Interface: `wlan0`
- MAC address: `2c:cf:67:6b:1e:92`
- State when profiled: disconnected
- NetworkManager reports Wi-Fi as available but not connected.

### Tailscale

Tailscale is installed and active.

- Interface: `tailscale0`
- IPv4 address: `100.79.203.47/32`
- IPv6 address: `fd7a:115c:a1e0::a501:cb98/128`
- NetworkManager treats the interface as externally managed.

Tailscale should be considered a normal remote-access path for this host.

## Containers

Docker is installed and in active use.

Evidence at profiling time includes:

- `docker0`
- multiple Docker bridge networks
- multiple active `veth` interfaces
- `patrick` belongs to the `docker` group

Observed Docker bridge subnets included:

- `172.17.0.0/16`
- `172.18.0.0/16`
- `172.19.0.0/16`
- `172.20.0.0/16`
- `172.21.0.0/16`
- `172.22.0.0/16`
- `172.23.0.0/16`
- `172.24.0.0/16`
- `172.25.0.0/16`

Do not assume these exact bridge IDs or container interfaces are stable across Docker restarts or redeployments.

## SSH and remote administration

- OpenSSH server: enabled
- OpenSSH server state when profiled: active
- Administrative user: `patrick`
- `patrick` UID: `1000`
- `patrick` GID: `1000`
- `patrick` has sudo privileges.
- `patrick` belongs to the `docker` group and can therefore manage Docker without `sudo`.

Observed supplementary groups:

- `adm`
- `dialout`
- `cdrom`
- `sudo`
- `audio`
- `plugdev`
- `games`
- `users`
- `input`
- `netdev`
- `spi`
- `i2c`
- `gpio`
- `lpadmin`
- `docker`

## Time and RTC

- Timezone: `Asia/Bangkok`
- Timezone abbreviation: ICT
- UTC offset: +07:00
- Daylight saving time: not used
- NTP synchronization: active
- Hardware RTC: present and reporting UTC
- RTC is not configured to local time.

## Firmware / thermal status

`vcgencmd` requires elevated privileges on this installation and works with `sudo`.

Observed firmware information:

- Firmware build date: 2026-02-06 14:31:40
- Firmware version: `8124798b`
- Firmware build type: `release`
- Firmware variant: `embedded`
- Throttling status when profiled: `throttled=0x0`
- CPU/GPU temperature when profiled: 47.7 °C

`throttled=0x0` means no current or historical undervoltage or throttling flags were reported at profiling time.

Use `sudo` for Raspberry Pi firmware queries on this machine:

```bash
sudo vcgencmd version
sudo vcgencmd get_throttled
sudo vcgencmd measure_temp
```

## Useful health commands

```bash
uptime
free -h
df -hT
ip -brief address
nmcli device status
tailscale status
docker ps
docker system df
systemctl --failed
```

If `vcgencmd` is repaired or available with elevated privileges, also use:

```bash
sudo vcgencmd version
sudo vcgencmd get_throttled
sudo vcgencmd measure_temp
```

## AI assistant guidance

When providing commands or configuration specifically for `hal2025`:

- Assume Debian 12 (`bookworm`), not Debian 13.
- Assume a Raspberry Pi 5 Model B Rev 1.0.
- Assume a 64-bit ARM (`arm64` / `aarch64`) system.
- Use `patrick` as the administrative user.
- Use `sudo` for privileged operating-system operations.
- Prefer `apt` for operating-system packages.
- Treat NetworkManager as the network configuration layer and prefer `nmcli` for host-side network changes.
- Ethernet address is `192.168.1.21/24`.
- Ethernet routing is statically configured.
- Tailscale is installed and active.
- Docker is installed and actively used.
- `patrick` is a member of the `docker` group.
- `/mnt/storage` is an NFSv4 mount backed by `192.168.1.201:/mnt/storage`.
- Primary local storage is NVMe, not microSD.
- The root filesystem resides on `/dev/nvme0n1p2`.
- Use `/boot/firmware` rather than legacy `/boot` paths.
- Prefer `Asia/Bangkok` as the system timezone.
- The machine has a hardware RTC.
- Take the 4 GB RAM capacity into account when recommending workloads.
- Remember that binaries installed or compiled for x86-64 will not run natively; this system requires ARM64-compatible software.
- GPIO/SPI/I2C-capable hardware is present and the `patrick` user belongs to the corresponding access groups.
- Do not assume Wi-Fi is connected.
- Do not rely on current Docker bridge IDs, veth device names, disk usage, or IP state without re-querying when exact current state matters.
- Use `sudo vcgencmd ...` for Raspberry Pi firmware, throttling, and temperature queries.
- Avoid storing or repeating the board serial number unless explicitly needed for diagnostics or identification.

## Useful identification commands

```bash
hostnamectl
cat /sys/firmware/devicetree/base/model; echo
grep -E '^(Model|Revision)' /proc/cpuinfo
uname -a
cat /etc/os-release
free -h
lsblk -f
ip -brief address
ip -brief link
nmcli device status
timedatectl
tailscale status
docker ps
```

## Profile provenance

This profile combines:

1. Facts observed directly from `hal2025` on 2026-08-20.
2. Stable Raspberry Pi 5 Model B hardware capabilities.

Values describing current state — IP addresses, disk usage, kernel, connection state, service state, Docker networks, container interfaces, and package versions — may change and should be re-queried when exact current-state information matters.
