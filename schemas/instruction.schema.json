{
  "$comment": "Generated from scripts/ai.ts. Do not edit manually.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "name": {
      "description": "Display name shown in the UI. Defaults to the file name.",
      "type": "string",
      "minLength": 1
    },
    "description": {
      "description": "Short description shown on hover in the Chat view.",
      "type": "string",
      "minLength": 1
    },
    "applyTo": {
      "description": "Glob pattern that defines which files the instructions apply to automatically, relative to the workspace root. Use ** to apply to all files. If not specified, the instructions are not applied automatically, but you can still add them manually to a chat request.",
      "type": "string"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "version": {
      "type": "string"
    },
    "source": {
      "type": "string"
    },
    "references": {
      "minItems": 1,
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "src": {
            "type": "string",
            "format": "uri",
            "pattern": "^https?:\\/\\/"
          },
          "note": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "src"
        ],
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
